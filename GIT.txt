GIT

git config --global user.email "email"
git config --global user.name "name"

git status
git add
git commit
git log
git checkout // cancelar cambios
git diff archivo.extension // revisar cambios
git commit -m "mensaje"
git branch fork
git checkout fork
git remote add origin URL
git push -u origin master
